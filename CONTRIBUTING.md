# Contributing

- Editorial fixes, examples, and clarifications: open a pull request.
- Changes to the specification, a binding, or the conformance surface: open a BEP under `proposals/` (see `BEP-0001-process.md`). The BEP and the specification text change in the same pull request.
- Adding an implementation: add a row to `registry/renderers.md` and a statement under `conformance/statements/` with a `conformance.json` in your repository.
- Style: normative language uses MUST, SHOULD, MAY (RFC 2119). Lists that define conformance (Core components, Core modifiers, hooks, environment keys) are generated by the conformance test, not typed by hand.

By contributing you agree that your contribution is licensed under the Apache License 2.0.
