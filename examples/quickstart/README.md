# Quickstart (planned)

One component, three platforms, five minutes: a `ProductCard` written once, rendered with `@metabindai/bindjs-react` in the browser, with `bindjs-apple` in a SwiftUI preview, and with `bindjs-android` in a Compose preview. Arrives with the first release.
