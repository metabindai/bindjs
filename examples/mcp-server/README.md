# MCP server example (planned)

A small TypeScript MCP server that registers one tool with a `ui://` View available as `application/bindjs+json` and as `text/html;profile=mcp-app`, chosen by the host's `mimeTypes`. Shows the View document, the package resource with a digest, and the HTML fallback. Arrives with the first release.
